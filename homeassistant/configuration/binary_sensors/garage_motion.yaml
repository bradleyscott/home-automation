platform: template
sensors:
  garage_motion:
    friendly_name: Garage motion
    device_class: motion
    value_template: "{{ states('sensor.garage_phone_motion') | float > 750 }}"
    delay_off:
      minutes: 2
