cam_view:
  view: yes
  icon: mdi:camcorder
  entities:
    - group.doorbell_camera
    - group.entrance_camera
    - group.garage_camera
    - group.pancam_camera
    - group.cubecam_camera

pancam_camera:
  name: Garage camera
  entities:
    - camera.pan_cam

cubecam_camera:
  name: Bedroom camera
  entities:
    - camera.cube_cam

doorbell_camera:
  name: Doorbell camera
  entities:
    - camera.front_door_doorbell

entrance_camera:
  name: Entrance camera
  entities:
    - camera.entrance_phone

garage_camera:
  name: Garage phone
  entities:
    - camera.garage_phone